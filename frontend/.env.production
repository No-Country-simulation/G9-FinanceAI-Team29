# Overrides para el build de producción (detrás de Caddy, same-origin).
# El resto de las VITE_* (Supabase, etc.) se toman de frontend/.env.
# Vite mergea .env + .env.production en el build de producción (npm run build).

VITE_API_URL=/api
VITE_AI_URL=/ai
