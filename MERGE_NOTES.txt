FinSightAI - merge final

Base utilizada: proyecto de Felipe (FinSightIA 2).
Integraciones del proyecto de Eddie:
- Registro de usuarios en Spring Boot (POST /api/usuarios).
- Campos nombre, apellido, email y authUserId.
- Asociación Supabase UUID -> usuarioId mediante localStorage.
- Home con espera de autenticación y bienvenida para usuarios sin movimientos.
- Corrección: para usuarios nuevos no se consulta /transacciones/resumen hasta que existan transacciones.
- Se preservaron las funciones de metas y las mejoras visuales del proyecto de Felipe.

Variables de entorno:
- frontend/.env ya está incluido desde el proyecto proporcionado.
- AI-Service/.env ya está incluido desde el proyecto proporcionado.
Revisar las claves antes de publicar el repositorio; no conviene subir archivos .env a GitHub.

Validación en este entorno:
- No se pudo completar npm install porque el registro interno no tenía el paquete yocto-queue 0.1.0.
- No se pudo ejecutar Maven porque mvn no está instalado en este entorno.
Esto no implica un error del proyecto; ejecutar las comprobaciones localmente.

CORRECCIÓN CHATBOT USUARIOS REALES (31 JUL 2026)
- El AI-Service ahora consulta Spring Boot para obtener las transacciones del usuario.
- Endpoint usado: GET {BACKEND_URL}/usuarios/{usuarioId}/transacciones
- USR1001 y los futuros usuarios ya no necesitan existir en usuarios_features.csv.
- Los CSV procesados quedan como respaldo para las cuentas demo si Spring no está disponible.
- Se normalizan tipo, monto, fecha, categoria, medioPago y recurrente.
- Se generan métricas mensuales y contexto financiero a partir de las transacciones reales.
- Configuración local: BACKEND_URL=http://localhost:8081/api
